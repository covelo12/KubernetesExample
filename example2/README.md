This is an expansion on the previous exercise to understand syntax and use behind volumes
1- Apply both files (pod and service)
2- Run test.sh to run a test pod for debug
3- Run the commented code in test.sh inside the debug pod and then run the created file
4- run bash script to cat the log file

The pod will write for the volume instead that is defined in the deployment definition.
